Runs a set of optimisation jobs upon the document, accepts a JSON string matching the "optimisation" option in [`oxvgrc.json`](https://github.com/noahbald/oxvg/wiki/Configuration). Also has the effect of the "Forget" action.

```sh
# Effects: History, Document
-optimise "<config>"
-optimize "<config>"
```
