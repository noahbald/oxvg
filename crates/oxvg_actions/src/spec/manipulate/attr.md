Sets the attribute of selected elements to the given value. Has no effect if the element doesn't accept the attribute as part of their [content-model](https://svgwg.org/svg2-draft/eltindex.html).

```sh
# Effects: History, Document
-attr "<qual-name>" "<value>"
```
