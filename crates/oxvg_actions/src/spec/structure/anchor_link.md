Wraps each selected element in an [anchor link element](https://developer.mozilla.org/en-US/docs/Web/SVG/Reference/Element/a). Adjacent selections will be grouped within the same link. Selection moved to links.

```sh
# Effects: history, document, selection, tree
-anchor-link "<href>"
```
