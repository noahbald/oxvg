Wraps each selected element in a [group element](https://developer.mozilla.org/en-US/docs/Web/SVG/Reference/Element/g). Adjacent selections will be grouped within the same element. Selection moved to groups.

```sh
# Effects: history, document, selection, tree
-group
```
