Wraps each selected element in the given element. Adjacent selections will be grouped within the same element. Selection moved to the created elements.

```sh
# Effects: history, document, selection, tree
-wrap "<qual-name>"
```
